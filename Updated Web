<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>FailSafe Solution</title>

  <meta name="description" content="FailSafe is a packaging-level, controlled-access pharmaceutical safety cap designed to restrict physical access to prescription medications between scheduled dosing windows." />

  <style>
    :root{
      --bg:#ffffff;
      --text:#0b0b0f;
      --muted:#5c5f6a;
      --card:rgba(0,0,0,.03);
      --shadow:0 20px 60px rgba(0,0,0,.10);
      --shadow2:0 10px 26px rgba(0,0,0,.08);
      --radius:20px;
      --max:1120px;
    }

    *{ box-sizing:border-box; }
    html{ scroll-behavior:smooth; }

    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, "SF Pro Display", "SF Pro Text",
        Segoe UI, Roboto, Arial, sans-serif;
      color:var(--text);
      background:var(--bg);
      line-height:1.45;
    }

    a{ color:inherit; text-decoration:none; }
    .wrap{ max-width:var(--max); margin:0 auto; padding:0 22px; }

    /* NAV */
    .nav{
      position:sticky;
      top:0;
      z-index:50;
      background:rgba(255,255,255,.85);
      backdrop-filter:blur(14px);
      border-bottom:1px solid rgba(0,0,0,.08);
    }
    .navInner{
      display:flex;
      align-items:center;
      justify-content:space-between;
      height:88px;
      gap:16px;
    }

    .brand{
      display:flex;
      align-items:center;
    }

    .brandLogo{
      height:64px;
      width:auto;
      display:block;
    }

    .links{
      display:flex;
      gap:18px;
      font-size:14px;
      color:rgba(0,0,0,.7);
    }

    .navCta{
      display:flex;
      gap:10px;
    }

    /* BUTTONS */
    .btn{
      padding:9px 14px;
      border-radius:999px;
      font-size:13px;
      border:1px solid rgba(0,0,0,.14);
      background:#fff;
      cursor:pointer;
    }

    .btnPrimary{
      background:#0b0b0f;
      color:#fff;
      border-color:#0b0b0f;
    }

    /* HERO */
    .hero{
      padding:72px 0 48px;
      background:
        radial-gradient(900px 380px at 20% 10%, rgba(0,0,0,.05), transparent 60%),
        radial-gradient(900px 380px at 80% 0%, rgba(0,0,0,.04), transparent 62%),
        #fff;
    }

    .heroGrid{
      display:grid;
      grid-template-columns:1.1fr 0.9fr;
      gap:34px;
      align-items:center;
    }

    .eyebrow{
      display:inline-block;
      font-size:12px;
      color:rgba(0,0,0,.6);
      border:1px solid rgba(0,0,0,.1);
      padding:8px 12px;
      border-radius:999px;
      margin-bottom:16px;
    }

    h1{
      font-size:clamp(36px,5vw,60px);
      line-height:1.05;
      margin:0 0 14px;
    }

    .sub{
      font-size:18px;
      color:var(--muted);
      max-width:60ch;
    }

    .heroActions{
      margin-top:20px;
      display:flex;
      flex-wrap:wrap;
      gap:10px;
    }

    .shot{
      width:100%;
      max-width:420px;
      border-radius:28px;
      border:1px solid rgba(0,0,0,.1);
      box-shadow:var(--shadow);
      background:#fff;
    }

    /* SECTIONS */
    .section{ padding:56px 0; }

    .sectionHeader h2{
      margin:0 0 6px;
      font-size:28px;
    }

    .sectionHeader p{
      margin:0;
      color:var(--muted);
      max-width:70ch;
    }

    .grid{
      display:grid;
      grid-template-columns:repeat(12,1fr);
      gap:14px;
      margin-top:20px;
    }

    .card{
      grid-column:span 4;
      background:var(--card);
      border:1px solid rgba(0,0,0,.08);
      border-radius:var(--radius);
      padding:18px;
      box-shadow:0 8px 24px rgba(0,0,0,.06);
    }

    .split{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:14px;
      margin-top:20px;
    }

    .panel{
      background:#fff;
      border:1px solid rgba(0,0,0,.08);
      border-radius:var(--radius);
      padding:18px;
      box-shadow:var(--shadow2);
    }

    /* FOOTER */
    footer{
      padding:26px 0 40px;
      border-top:1px solid rgba(0,0,0,.06);
      font-size:12px;
      color:rgba(0,0,0,.55);
    }

    /* RESPONSIVE */
    @media (max-width:980px){
      .heroGrid{ grid-template-columns:1fr; }
      .links{ display:none; }
      .card{ grid-column:span 6; }
      .split{ grid-template-columns:1fr; }
    }

    @media (max-width:520px){
      .brandLogo{ height:72px; }
      .navInner{ height:96px; }
      .card{ grid-column:span 12; }
    }
  </style>
</head>

<body>

  <div class="nav">
    <div class="wrap navInner">
      <a class="brand" href="#top">
        <img class="brandLogo" src="failsafe-solution.png" alt="FailSafe Solution logo">
      </a>

      <div class="links">
        <a href="#how">How it works</a>
        <a href="#sourcing">Sourcing</a>
        <a href="#populations">Populations</a>
        <a href="#founder">Founder</a>
      </div>

      <div class="navCta">
        <a class="btn" href="NewWhitepaper.pdf" target="_blank">Whitepaper</a>
        <a class="btn btnPrimary" href="mailto:failsafeworld@gmail.com">Request info</a>
      </div>
    </div>
  </div>

  <header class="hero" id="top">
    <div class="wrap heroGrid">
      <div>
        <div class="eyebrow">Controlled-access packaging • Safety between doses</div>
        <h1>Access control, built into the cap.</h1>
        <p class="sub">
          FailSafe is a packaging-level, controlled-access pharmaceutical safety cap designed to restrict physical access to prescription medications between scheduled dosing windows.
        </p>
        <div class="heroActions">
          <a class="btn btnPrimary" href="mailto:failsafeworld@gmail.com">Request information</a>
          <a class="btn" href="NewWhitepaper.pdf" target="_blank">Read the whitepaper</a>
        </div>
      </div>

      <div>
        <img class="shot" src="product.jpg" alt="FailSafe prototype">
      </div>
    </div>
  </header>

  <footer>
    <div class="wrap">
      © 2026 FailSafe. All rights reserved. ·
      <a href="mailto:failsafeworld@gmail.com">failsafeworld@gmail.com</a>
    </div>
  </footer>

</body>
</html>
